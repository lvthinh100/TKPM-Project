<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">QLKS</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav w-100">
        <li class="nav-item">
          {{#if user}}
            <a
              class="nav-link active"
              aria-current="page"
              href="/favorites"
            >Favorite</a>
          {{/if}}
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/rooms">All rooms</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/about">All About</a>
        </li>
        <li class="nav-item d-block ms-auto">

          {{! Co user thi ra logout khong co thi ra auth }}
          {{#if user}}
            <form action="/api/user/logout" method="post" class="m-0">
              <span>Hello {{user.name}} </span>
              <input
                class="btn btn-primary ms-1"
                type="submit"
                value="Logout"
              />
            </form>
          {{else}}
            <a class="btn btn-primary" href="/auth">Login</a>

          {{/if}}
        </li>
      </ul>
    </div>
  </div>
</nav>